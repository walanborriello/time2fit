# .htaccess per PRODUZIONE (OVH Cloud)
# Copia questo file come .htaccess nella root SOLO se il virtualhost non punta a public/

# Redirect tutto a public/ se il virtualhost non punta già a public/
RewriteEngine On

# Se la richiesta non è già in public/, reindirizza
RewriteCond %{REQUEST_URI} !^/public/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /public/$1 [L]

