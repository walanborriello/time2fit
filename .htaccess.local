# .htaccess per SVILUPPO LOCALE (Apache)
# NOTA: In locale usi Docker con Nginx, quindi questo file NON serve
# Usa questo file SOLO se testi con Apache locale invece di Docker

# Redirect tutto a public/
RewriteEngine On

# Se la richiesta non è già in public/, reindirizza
RewriteCond %{REQUEST_URI} !^/public/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ /public/$1 [L]

